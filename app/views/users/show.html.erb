<div class="background-profile">

</div>
<div class="backround-user">
  <div class="profile-info">
  <%= image_tag "profilephoto.png", class: "", alt: "alttext" %>
  <h2><%=  @user.name%></h2>
  <p>⭐⭐⭐⭐⭐</p>
  <p>london, england</p>
  </div>
<div class="profile-links">

  <div class="clothes">
    <%= link_to "clothes", user_path(@user), class: 'clothes' %>
  </div>
  <div class="likes">
    <%= link_to "likes", user_favorites_path(@user)%>
  </div>
  <% if  @user.articles%>
  <div class="likes">
    <%= link_to "reviews", user_reviews_path(@user)%>
  </div>
  <% end %>
</div>

</div>


    <div class="clothing-grid">
  <% @user.articles.each do |article| %>
  <%= link_to article_path(article) do %>
  <%= cl_image_tag article.photo.key, class: "article-photo" %>
  <% end %>

  <% end %>
  </div>







