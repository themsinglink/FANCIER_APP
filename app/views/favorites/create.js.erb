function addToFavorites() {
  var star = document.querySelector(`#action-article-<%= @favorite.article.id %>`);
  star.innerHTML = '<%= j render "toggle_favorite_action", article: @favorite.article, user: current_user %>' ;
}

addToFavorites();
